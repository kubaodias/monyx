node_modules/
dist/
.DS_Store

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/.kotlin/
*.apk
*.aab
*.keystore
*.jks
keystore.properties
# The assistant's phone number. Kept out of the repo because a phone
# number is personal data; the call button hides itself without it.
android/voice.properties

# Telnyx generated types (regenerate with `telnyx-edge types`)
server/telnyx-env.d.ts

# Local backups / exports
*.sql.gz
backups/

# Secrets
.wallet.token
